types = List(
  new Dynamic {
    name = "Echo"
    doc = "Echo resource"
    activeVersion = "v1"
    versions = List(
      new Dynamic {
        version = "v1"
        served = true
        actions = List(
          new Dynamic {
            name = "List"
            doc = "List echoes"
            privilege = "Read"
            effect = "Stateless"
            idempotent = true
            openApiMapping = new Dynamic { operationId = "listEcho" }
          }
        )
      }
    )
  }
)
